Banking Demo installed.
Architecture: Caddy (edge, hostNetwork) → Kong (ClusterIP) → API Producer → NATS → Consumers

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

Deploy command:
  helm upgrade --install {{ .Release.Name }} . \
    -n {{ .Release.Namespace }} --create-namespace \
    -f values.yaml -f values.local.yaml --wait

{{ if .Values.caddy.tls.enabled }}
TLS domain: {{ .Values.caddy.tls.domain }}
{{ else }}
No TLS configured — HTTP-only mode (access via http://<node-ip>).
{{ end }}
