apiVersion: v2
name: banking-demo
description: Banking Demo Golang — Kong → API Producer → NATS → Consumers
type: application
version: 0.3.0
appVersion: "9"
keywords:
  - banking
  - microservices
  - kong
  - nats
  - redis
# Helm 4 defaults to Server-Side Apply (SSA) on fresh installs; SSA is GA since k8s 1.25.
kubeVersion: ">=1.25"
