---
# defaults for the common role â€” override in group_vars or host_vars
swap_size_mb: 2048
sysctl_settings:
  # Allow more open files (needed by k3s + NATS)
  fs.file-max: 262144
  # Improve TCP performance
  net.core.somaxconn: 1024
  net.ipv4.tcp_max_syn_backlog: 1024
